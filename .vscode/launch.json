{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "MineSweeper Debug",
            "program": "${workspaceFolder}/mineSweeper",
            "args": [],
            "cwd": "${workspaceFolder}",
            // "preLaunchTask": "build"
        }
    ]
}
